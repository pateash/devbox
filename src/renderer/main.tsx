import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { QueryClient, QueryClientProvider, useQuery } from '@tanstack/react-query'
import { Archive, Blocks, Boxes, ChevronDown, CircleDot, ExternalLink, Github, GitPullRequest, LayoutGrid, Moon, Plus, RefreshCw, Search, Settings, Sun } from 'lucide-react'
import type { GitHubIntegrationSummary, JiraIntegrationSummary, JiraProject, Theme, WorkItem, Workspace, WorkspaceSummary } from '../shared/contracts'
import { createBrowserDevboxApi } from './browser-api'
import './styles.css'

const client = new QueryClient()
type Page = 'Overview' | 'Integrations' | 'GlobalIntegrations' | 'GitHub' | 'GitIntegration' | 'Jira' | 'Bitbucket' | 'GlobalSettings' | 'WorkspaceSettings'
const MIN_SIDEBAR_WIDTH = 240
const MAX_SIDEBAR_WIDTH = 480
const DEFAULT_SIDEBAR_WIDTH = 308
const workspaceSections: Array<{ label: 'Git' | 'Tickets'; page: 'GitHub' | 'Jira'; icon: typeof Github }> = [{ label: 'Git', page: 'GitHub', icon: GitPullRequest }, { label: 'Tickets', page: 'Jira', icon: CircleDot }]
const integrationCatalog: Array<{ id: 'github' | 'bitbucket' | 'jira'; name: string; category: 'Git' | 'Tickets'; description: string; icon: typeof Github; available: boolean }> = [
  { id: 'github', name: 'GitHub', category: 'Git', description: 'Review requests, failing checks, and mentions in one local attention view.', icon: Github, available: true },
  { id: 'bitbucket', name: 'Bitbucket', category: 'Git', description: 'Pull requests and build activity from Bitbucket workspaces.', icon: LayoutGrid, available: false },
  { id: 'jira', name: 'Jira', category: 'Tickets', description: 'Assigned work from your configured Jira projects.', icon: CircleDot, available: true }
]

function workspaceInitials(name: string): string { return name.split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]).join('').toUpperCase() || 'DB' }
function relativeRefreshTime(value: string | null): string { if (!value) return 'Not refreshed yet'; const seconds=Math.max(0,Math.round((Date.now()-new Date(value).getTime())/1000)); if(seconds<60) return 'Refreshed just now'; const minutes=Math.floor(seconds/60); if(minutes<60) return `Refreshed ${minutes} ${minutes===1?'minute':'minutes'} ago`; const hours=Math.floor(minutes/60); if(hours<24) return `Refreshed ${hours} ${hours===1?'hour':'hours'} ago`; const days=Math.floor(hours/24); return `Refreshed ${days} ${days===1?'day':'days'} ago` }
function AppMark({ className = '' }: { className?: string }): React.ReactElement { return <img className={'app-mark ' + className} src="./devbox-icon.png" alt="DevBox" /> }

function WorkspaceForm({ current, done, cancel }: { current?: Workspace; done: () => void; cancel?: () => void }): React.ReactElement {
  const [name, setName] = useState(current?.name ?? '')
  const [error, setError] = useState('')
  useEffect(() => { setName(current?.name ?? ''); setError('') }, [current?.id])
  async function submit(event: React.FormEvent): Promise<void> { event.preventDefault(); try { if (current) await window.devbox.workspaces.rename(current.id, { name }); else await window.devbox.workspaces.create({ name }); done() } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to save workspace.') } }
  return <form className="workspace-form" onSubmit={(event) => void submit(event)}><label htmlFor="workspace-name">{current ? 'Rename workspace' : 'New workspace'}</label><input id="workspace-name" aria-label="Workspace name" autoFocus value={name} onChange={(event) => setName(event.target.value)} maxLength={80} placeholder="Workspace name" /><div className="form-actions"><button type="submit">Save</button>{cancel && <button type="button" className="secondary" onClick={cancel}>Cancel</button>}</div>{error && <span className="error">{error}</span>}</form>
}

function WorkspaceRail({ current, workspaces, select, integrations, settings, theme, toggleTheme }: { current: Workspace; workspaces: WorkspaceSummary[]; select: (id: string) => void; integrations: () => void; settings: () => void; theme: Theme; toggleTheme: () => void }): React.ReactElement {
  const ThemeIcon = theme === 'dark' ? Sun : Moon
  const themeLabel = theme === 'dark' ? 'Use light mode' : 'Use dark mode'
  return <aside className="workspace-rail" aria-label="Workspaces"><div className="traffic-lights" aria-hidden="true"><i /><i /><i /></div><div className="workspace-avatars">{workspaces.slice(0, 5).map((workspace) => <button key={workspace.id} className={'workspace-avatar ' + (workspace.id === current.id ? 'selected' : '')} onClick={() => select(workspace.id)} aria-label={'Switch to ' + workspace.name} aria-pressed={workspace.id === current.id}>{workspaceInitials(workspace.name)}{workspace.id === current.id && <span className="presence" />}</button>)}</div><div className="rail-actions"><button className="rail-button" onClick={integrations} aria-label="Global integrations"><Blocks size={19} /></button><button className="rail-button rail-theme-button" onClick={toggleTheme} aria-label={themeLabel} title={themeLabel}><ThemeIcon size={19} /></button><button className="rail-button" onClick={settings} aria-label="Global settings"><Settings size={18} /></button></div></aside>
}

function WorkspaceMenu({ current, workspaces, creating, setCreating, select, done }: { current: Workspace; workspaces: WorkspaceSummary[]; creating: boolean; setCreating: (value: boolean) => void; select: (id: string) => void; done: () => void }): React.ReactElement {
  if (creating) return <div className="workspace-menu" role="dialog" aria-label="Create workspace"><WorkspaceForm done={done} cancel={() => setCreating(false)} /></div>
  return <div className="workspace-menu" role="dialog" aria-label="Switch workspace"><p className="workspace-menu-title">Switch workspace</p>{workspaces.map((workspace) => <button className={'menu-action ' + (workspace.id === current.id ? 'selected' : '')} key={workspace.id} onClick={() => select(workspace.id)}>{workspaceInitials(workspace.name)}<span>{workspace.name}</span></button>)}<div className="menu-divider" /><button className="menu-action create-workspace-action" onClick={() => setCreating(true)}><Plus size={16} />Create workspace</button></div>
}

function Sidebar({ current, workspaces, page, integration, jira, assignedRepositories, menuOpen, creating, setMenuOpen, setCreating, setPage, select, done }: { current: Workspace; workspaces: WorkspaceSummary[]; page: Page; integration?: GitHubIntegrationSummary; jira?: JiraIntegrationSummary; assignedRepositories: string[]; menuOpen: boolean; creating: boolean; setMenuOpen: (value: boolean) => void; setCreating: (value: boolean) => void; setPage: (page: Page) => void; select: (id: string) => void; done: () => void }): React.ReactElement {
  const gitConfigured = assignedRepositories.length > 0
  const ticketsConfigured = !!jira?.projects.length
  return <aside className="workspace-sidebar">
    <div className="sidebar-heading"><button className="workspace-switcher" onClick={() => setMenuOpen(!menuOpen)} aria-expanded={menuOpen}><span>{current.name}</span><ChevronDown size={18} /></button><button className="compose-button" onClick={() => setPage('WorkspaceSettings')} aria-label="Workspace settings"><Settings size={19} /></button>{menuOpen && <WorkspaceMenu current={current} workspaces={workspaces} creating={creating} setCreating={setCreating} select={select} done={done} />}</div>
    <button className="sidebar-search" onClick={() => setPage('Integrations')}><Search size={18} />Find a section or integration<span>⌘K</span></button>
    <nav className="primary-nav" aria-label="Workspace navigation"><button className={page === 'Overview' ? 'active' : ''} onClick={() => setPage('Overview')}><Boxes size={18} />Overview</button>{workspaceSections.map(({ label, page: sectionPage, icon: Icon }) => <button key={label} className={page === sectionPage ? 'active' : ''} onClick={() => setPage(sectionPage)}><Icon size={18} />{label}<span className="unread-count">{(sectionPage === 'GitHub' ? gitConfigured : ticketsConfigured) ? '•' : ''}</span></button>)}</nav>
    <div className="sidebar-divider" />
    <div className="tools-heading"><button className={page === 'Integrations' ? 'active' : ''} onClick={() => setPage('Integrations')}><Blocks size={19} />Integrations</button><button className="add-integration-button" onClick={() => setPage('Integrations')} aria-label="Configure integrations"><Plus size={16} /></button></div>
    <div className="integration-summary" aria-label="Workspace integration status"><button type="button" className={gitConfigured ? 'configured' : ''} onClick={() => setPage('GitIntegration')}><b>GitHub{integration ? ` · ${integration.displayName}` : ''}</b><small>{gitConfigured ? assignedRepositories[0] : integration ? 'Choose one repository' : 'Not connected'}</small></button><button type="button" className={ticketsConfigured ? 'configured' : ''} onClick={() => setPage('Jira')}><b>Jira</b><small>{ticketsConfigured ? jira?.projects.map((project) => project.key).join(', ') : jira ? 'Choose projects' : 'Not connected'}</small></button></div>
    <div className="sidebar-footer"><span>Local-first</span></div>
  </aside>
}

function GitHubConnect({ integration, workspaceId, assignedRepositories, refresh }: { integration?: GitHubIntegrationSummary; workspaceId?: string; assignedRepositories: string[]; refresh: () => Promise<unknown> }): React.ReactElement {
  const [token, setToken] = useState('')
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)
  const [refreshMessage, setRefreshMessage] = useState('')
  const [reconnecting, setReconnecting] = useState(false)
  const [repositoryQuery, setRepositoryQuery] = useState('')
  const [selectedRepositories, setSelectedRepositories] = useState<string[]>(assignedRepositories)
  useEffect(() => { setSelectedRepositories(assignedRepositories.slice(0,1)); setRepositoryQuery('') }, [workspaceId, assignedRepositories])
  async function connect(event: React.FormEvent): Promise<void> { event.preventDefault(); setBusy(true); setError(''); try { await window.devbox.github.connectPersonalAccessToken(token); setToken(''); await refresh() } catch (reason) { setError(reason instanceof Error ? reason.message : 'GitHub could not verify that token.') } finally { setBusy(false) } }
  async function saveRepositories(): Promise<void> { if (!workspaceId) return; setBusy(true); setError(''); try { await window.devbox.github.setAssignedRepositories(workspaceId, selectedRepositories); await refresh() } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to update repository assignments.') } finally { setBusy(false) } }
  async function refreshRepositories(): Promise<void> { setBusy(true); setError(''); setRefreshMessage(''); try { await window.devbox.github.refreshGlobal(); await refresh(); setRefreshMessage('Repository list refreshed just now.') } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to refresh repositories.') } finally { setBusy(false) } }
  if (integration) {
    const matchingRepositories = integration.repositories.filter((repository) => repository.fullName.toLowerCase().includes(repositoryQuery.trim().toLowerCase()))
    const needsReconnect=integration.state==='needs_reauth'||integration.lastError?.includes('safeStorage.decryptString')
    return <><section className="integration-panel"><div className="provider-icon github"><Github size={28} /></div><div className="integration-copy"><p>{needsReconnect ? 'Reconnect required' : 'Connected globally'}</p><h2>GitHub · {integration.displayName}</h2><span>{needsReconnect ? 'GitHub access needs reconnecting before repositories can refresh.' : integration.lastError ?? relativeRefreshTime(integration.lastSyncedAt)}</span></div><div className="integration-actions"><button className="secondary" onClick={() => void refreshRepositories()} disabled={busy}><RefreshCw className={busy ? 'spin' : ''} size={16} />{busy ? 'Refreshing…' : 'Refresh repositories'}</button>{needsReconnect && <button className="secondary" onClick={() => setReconnecting(!reconnecting)} disabled={busy}>{reconnecting ? 'Cancel reconnect' : 'Reconnect GitHub'}</button>}<button className="danger-button" onClick={() => void window.devbox.github.disconnect().then(refresh)} disabled={busy}>Disconnect</button></div></section>{reconnecting && <form className="token-form reconnect-form" onSubmit={(event) => void connect(event)}><label htmlFor="github-reconnect-token">New personal access token</label><input id="github-reconnect-token" type="password" value={token} onChange={(event) => setToken(event.target.value)} autoComplete="off" spellCheck={false} placeholder="github_pat_…" required disabled={busy} /><p>Reconnecting replaces the unreadable token while keeping this workspace’s repository attachment.</p><button type="submit" disabled={busy}>{busy ? 'Reconnecting…' : 'Save and reconnect'}</button></form>}{refreshMessage && <p className="refresh-message">{refreshMessage}</p>}{workspaceId && <section className="repositories-panel"><div><p className="eyebrow">This workspace</p><h2>{selectedRepositories.length ? '1 GitHub repository attached' : 'Attach one GitHub repository'}</h2><p>Choose the repository whose pull requests and assigned issues belong in this workspace. You can change it later.</p><label className="repository-search"><Search size={16} /><input value={repositoryQuery} onChange={(event) => setRepositoryQuery(event.target.value)} placeholder="Search repositories" aria-label="Search repositories" /></label></div><div className="repository-picker">{matchingRepositories.length ? <div className="repository-options">{matchingRepositories.map((repository) => <label key={repository.fullName}><input type="radio" name="workspace-repository" checked={selectedRepositories[0]===repository.fullName} onChange={() => { setSelectedRepositories([repository.fullName]); setError('') }} disabled={busy} /><Github size={15} />{repository.fullName}</label>)}</div> : <p className="repository-empty">No repositories match that search. Refresh repositories to fetch recent GitHub changes.</p>}<div className="repository-selection-actions"><button type="button" className="secondary" onClick={() => setSelectedRepositories([])} disabled={busy || selectedRepositories.length===0}>Clear selection</button><button onClick={() => void saveRepositories()} disabled={busy}>{busy ? 'Saving and syncing…' : 'Save repository and sync'}</button></div>{selectedRepositories.length === 0 && <p className="repository-empty">No repository is attached yet.</p>}</div></section>}{error && <p className="error">{error}</p>}</>
  }
  return <section className="integration-empty"><div className="provider-icon github"><Github size={32} /></div><h2>Bring GitHub into this workspace</h2><p>Use a fine-grained personal access token. DevBox encrypts it in macOS secure storage and only uses it to sync your selected GitHub data.</p><button className="secondary external-link" type="button" onClick={() => void window.devbox.links.openExternal('https://github.com/settings/personal-access-tokens/new')}>Create a GitHub token<ExternalLink size={15} /></button><form className="token-form" onSubmit={(event) => void connect(event)}><label htmlFor="github-token">Personal access token</label><input id="github-token" type="password" value={token} onChange={(event) => setToken(event.target.value)} autoComplete="off" spellCheck={false} placeholder="github_pat_…" required disabled={busy} /><p>Grant access to the repositories you want DevBox to read, plus pull requests, checks, and notifications.</p><button type="submit" disabled={busy}>{busy ? 'Verifying token…' : 'Save and connect'}</button></form>{error && <p className="error">{error}</p>}</section>
}

function IntegrationDirectory({ integration, jira, assignedRepositories, openGitHub, openJira }: { integration?: GitHubIntegrationSummary; jira?: JiraIntegrationSummary; assignedRepositories: string[]; openGitHub: () => void; openJira: () => void }): React.ReactElement {
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState<'All' | 'Git' | 'Tickets'>('All')
  const matching = useMemo(() => integrationCatalog.filter((item) => (category === 'All' || item.category === category) && (item.name + item.category + item.description).toLowerCase().includes(query.trim().toLowerCase())), [category, query])
  const githubConfigured = assignedRepositories.length > 0
  const jiraConfigured = !!jira?.projects.length
  const unconfigured = matching.filter((item) => item.id === 'github' ? !githubConfigured : item.id === 'jira' ? !jiraConfigured : true)
  return <section className="integration-directory">
    <div className="directory-title"><p className="eyebrow">This workspace</p><h1>Integrations</h1><p>Attach the sources that define this workspace. GitHub repositories and Jira projects stay scoped here, while your credentials remain local to this Mac.</p></div>
    <label className="directory-search"><Search size={20} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search integrations by name or category" aria-label="Search integrations" /></label>
    <div className="directory-filters"><label>Category<select value={category} onChange={(event) => setCategory(event.target.value as 'All' | 'Git' | 'Tickets')}><option value="All">All categories</option><option value="Git">Git</option><option value="Tickets">Tickets</option></select></label></div>
    {(githubConfigured || jiraConfigured) && <section className="connected-integrations"><div className="directory-section-heading"><h2>Configured for this workspace</h2><span>{Number(githubConfigured)+Number(jiraConfigured)}</span></div>{githubConfigured && <article className="connected-integration"><span className="catalog-icon"><Github size={23} /></span><div><h3>GitHub</h3><p>{assignedRepositories.length} {assignedRepositories.length === 1 ? 'repository' : 'repositories'} attached to this workspace</p></div><button className="secondary" onClick={openGitHub}>Configure</button></article>}{jiraConfigured && <article className="connected-integration"><span className="catalog-icon"><CircleDot size={23} /></span><div><h3>Jira</h3><p>{jira?.projects.map((project) => project.key).join(', ')}</p></div><button className="secondary" onClick={openJira}>Configure</button></article>}</section>}
    <section><div className="directory-section-heading"><h2>{githubConfigured || jiraConfigured ? 'Add another integration' : 'Start with a source'}</h2><span>{unconfigured.length}</span></div><div className="integration-grid">{unconfigured.map((item) => { const Icon = item.icon; const open=item.id==='github'?openGitHub:openJira; const accountConnected=item.id==='github' ? !!integration : item.id==='jira' ? !!jira : false; return <article className="catalog-card" key={item.id}><span className="catalog-icon"><Icon size={25} /></span><div className="catalog-card-copy"><div><h3>{item.name}</h3><span className={item.available ? 'available-badge' : 'planned-badge'}>{accountConnected ? 'Account connected' : item.available ? item.category : 'Planned'}</span></div><p>{item.description}</p></div><button className={item.available ? 'catalog-connect' : 'secondary'} disabled={!item.available} onClick={item.available ? open : undefined}>{accountConnected ? item.id==='github' ? 'Choose repositories' : 'Choose projects' : item.available ? 'Connect' : 'Coming soon'}</button></article> })}</div>{unconfigured.length === 0 && <div className="catalog-no-results"><Search size={22} /><p>No integrations match that search.</p></div>}</section>
  </section>
}

function JiraConnect({ workspaceId, integration, refresh }: { workspaceId:string; integration?:JiraIntegrationSummary; refresh:()=>void }):React.ReactElement { const [deployment,setDeployment]=useState<'cloud'|'data-center'>('cloud'); const [baseUrl,setBaseUrl]=useState(''); const [email,setEmail]=useState(''); const [token,setToken]=useState(''); const [available,setAvailable]=useState<JiraProject[]>([]); const [selected,setSelected]=useState<JiraProject[]>([]); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); const connect=async(event:React.FormEvent):Promise<void>=>{event.preventDefault();setBusy(true);setError('');try{const result=await window.devbox.jira.connect({workspaceId,deployment,baseUrl,email:deployment==='cloud'?email:undefined,token});setToken('');setAvailable(result.projects);setSelected(result.projects)}catch(reason){setError(reason instanceof Error?reason.message:'Jira could not verify these credentials.')}finally{setBusy(false)}}; const save=async():Promise<void>=>{setBusy(true);setError('');try{await window.devbox.jira.setProjects(workspaceId,selected);setAvailable([]);refresh()}catch(reason){setError(reason instanceof Error?reason.message:'Unable to save Jira projects.')}finally{setBusy(false)}}; if(integration){return <><section className="integration-panel"><div className="provider-icon jira"><CircleDot size={28}/></div><div className="integration-copy"><p>{integration.deployment==='cloud'?'Jira Cloud':'Jira Data Center'}</p><h2>Jira · {integration.displayName}</h2><span>{integration.lastError??(integration.lastSyncedAt?'Last synced '+new Date(integration.lastSyncedAt).toLocaleString():'Choose projects to begin syncing')}</span></div><div className="integration-actions"><button className="secondary" onClick={()=>void window.devbox.jira.refresh(workspaceId).then(refresh)} disabled={busy}><RefreshCw size={16}/>Refresh</button><button className="danger-button" onClick={()=>void window.devbox.jira.disconnect(workspaceId).then(refresh)} disabled={busy}>Disconnect</button></div></section><section className="repositories-panel"><div><p className="eyebrow">This workspace</p><h2>Jira projects</h2><p>{integration.projects.length?integration.projects.map((project)=>project.key).join(', '):'No projects are selected yet.'}</p></div></section>{error&&<p className="error">{error}</p>}</>}; return <section className="integration-empty"><div className="provider-icon jira"><CircleDot size={32}/></div><h2>Bring Jira into this workspace</h2><p>DevBox reads only your assigned, non-terminal issues from selected projects. Credentials are encrypted in macOS secure storage.</p><form className="token-form" onSubmit={(event)=>void connect(event)}><label>Deployment<select value={deployment} onChange={(event)=>setDeployment(event.target.value as 'cloud'|'data-center')} disabled={busy}><option value="cloud">Jira Cloud</option><option value="data-center">Jira Data Center</option></select></label><label>Jira base URL<input type="url" value={baseUrl} onChange={(event)=>setBaseUrl(event.target.value)} placeholder="https://your-site.atlassian.net" required disabled={busy}/></label>{deployment==='cloud'&&<label>Account email<input type="email" value={email} onChange={(event)=>setEmail(event.target.value)} required disabled={busy}/></label>}<label>{deployment==='cloud'?'API token':'Personal access token'}<input type="password" value={token} onChange={(event)=>setToken(event.target.value)} autoComplete="off" required disabled={busy}/></label>{available.length===0?<button type="submit" disabled={busy}>{busy?'Verifying…':'Connect and choose projects'}</button>:<><p>Select at least one accessible project.</p><div className="repository-options">{available.map((project)=><label key={project.key}><input type="checkbox" checked={selected.some((item)=>item.key===project.key)} onChange={()=>setSelected((items)=>items.some((item)=>item.key===project.key)?items.filter((item)=>item.key!==project.key):[...items,project])} disabled={busy}/>{project.key} · {project.name}</label>)}</div><button type="button" onClick={()=>void save()} disabled={busy||!selected.length}>{busy?'Saving…':'Save projects and sync'}</button></>}</form>{error&&<p className="error">{error}</p>}</section> }
function PlannedTool({ name, category }: { name: string; category: string }): React.ReactElement { return <section className="planned-tool"><span className="coming-soon">Planned integration</span><h2>{name} is not connected yet</h2><p>DevBox’s {category} area is ready in the navigation, but this provider has not been implemented in this build. Its status is shown honestly so it cannot be mistaken for a live integration.</p><button className="secondary" disabled>Coming soon</button></section> }
function WorkRow({ item }: { item: WorkItem }): React.ReactElement { const ItemIcon = item.kind === 'pr' ? GitPullRequest : CircleDot; const label=item.kind==='pr'?'Pull request':item.kind==='jira-issue'?`Jira issue${item.status?' · '+item.status:''}`:'GitHub issue'; return <article className={'work-row ' + item.priority}><div className="row-source"><ItemIcon size={19} /></div><div className="row-copy"><p>{item.reason}</p><h3>{item.title}</h3><span>{label} · {item.repository} · Updated {new Date(item.updatedAt).toLocaleDateString()}</span></div><div className="row-action"><i aria-label={item.priority + ' priority'} /><button className="secondary" onClick={() => void window.devbox.links.openExternal(item.url)}>Open<ExternalLink size={15} /></button></div></article> }

function WorkView({ items, hasConfiguredSources, goToIntegrations }: { items: WorkItem[]; hasConfiguredSources: boolean; goToIntegrations: () => void }): React.ReactElement {
  return <><div className="content-title"><div><h1>Developer overview</h1><p>A quiet view of what needs your attention across this workspace.</p></div></div><section className="attention-section"><div className="section-heading"><h2>Needs your attention</h2>{items.length > 0 && <span>{items.length}</span>}</div>{items.length ? <div className="work-list">{items.map((item) => <WorkRow key={item.id} item={item} />)}</div> : <div className="empty-state"><Blocks size={31} /><h2>{hasConfiguredSources ? 'You’re all caught up' : 'Set up this workspace'}</h2><p>{hasConfiguredSources ? 'New pull requests and tickets from the sources attached here will appear when they need your attention.' : 'Connect GitHub repositories, Jira projects, or both to bring this workspace to life.'}</p><button onClick={goToIntegrations}>{hasConfiguredSources ? 'Manage integrations' : 'Add an integration'}</button></div>}</section></>
}

function GitView({ repository, integration, items, manageIntegration }: { repository?: string; integration?: GitHubIntegrationSummary; items: WorkItem[]; manageIntegration: () => void }): React.ReactElement {
  if (!repository) return <div className="empty-state"><Github size={31} /><h2>Attach a repository to Git</h2><p>Select one GitHub repository in Integrations to give this workspace a focused pull request and issue view.</p><button onClick={manageIntegration}>Configure GitHub</button></div>
  const needsReconnect = integration?.state === 'needs_reauth' || integration?.lastError?.includes('safeStorage.decryptString')
  return <><section className="repository-detail"><div className="repository-heading"><span className="provider-icon github"><Github size={26} /></span><div><p className="eyebrow">This workspace</p><h1>{repository}</h1><p>GitHub repository attached to this workspace</p></div></div><div className="repository-detail-actions"><span className={needsReconnect ? 'sync-status warning' : 'sync-status'}>{needsReconnect ? 'Reconnect GitHub to refresh' : relativeRefreshTime(integration?.lastSyncedAt ?? null)}</span><button className="secondary external-link" onClick={() => void window.devbox.links.openExternal(`https://github.com/${repository}`)}>Open repository<ExternalLink size={15} /></button><button className="secondary" onClick={manageIntegration}>Manage integration</button></div></section><section className="attention-section git-work-section"><div className="section-heading"><h2>Pull requests & issues</h2>{items.length > 0 && <span>{items.length}</span>}</div>{items.length ? <div className="work-list">{items.map((item) => <WorkRow key={item.id} item={item} />)}</div> : <div className="empty-state compact-empty"><GitPullRequest size={28} /><h2>No GitHub work needs attention</h2><p>Actionable pull requests and assigned issues from {repository} will appear here after the next successful sync.</p><button className="secondary" onClick={manageIntegration}>View integration</button></div>}</section></>
}

function BackupSettings({ githubName }: { githubName?: string }): React.ReactElement {
  const { data: status, refetch } = useQuery({ queryKey: ['backup-status'], queryFn: () => window.devbox.backups.status() })
  const [repositories, setRepositories] = useState<Array<{ id: number; fullName: string; defaultBranch: string }>>([])
  const [repositoryId, setRepositoryId] = useState('')
  const [acknowledged, setAcknowledged] = useState(false)
  const [token, setToken] = useState('')
  const [confirmation, setConfirmation] = useState('')
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState('')
  async function chooseRepository(): Promise<void> { setBusy(true); setError(''); try { const result=await window.devbox.backups.repositories(); setRepositories(result); if(result.length===0) setError('No private GitHub repositories with push access are available.'); } catch(reason) { setError(reason instanceof Error?reason.message:'Unable to load backup repositories.') } finally { setBusy(false) } }
  async function configure(): Promise<void> { setBusy(true); setError(''); try { await window.devbox.backups.configure(Number(repositoryId),true); await refetch() } catch(reason) { setError(reason instanceof Error?reason.message:'Unable to save backup settings.') } finally { setBusy(false) } }
  async function connectToken(event: React.FormEvent): Promise<void> { event.preventDefault(); setBusy(true); setError(''); try { setRepositories(await window.devbox.backups.connectToken(token)); setToken('') } catch(reason) { setError(reason instanceof Error?reason.message:'Unable to verify the backup token.') } finally { setBusy(false) } }
  async function backup(): Promise<void> { setBusy(true); setError(''); try { await window.devbox.backups.run(); await refetch() } catch(reason) { setError(reason instanceof Error?reason.message:'Backup failed.') } finally { setBusy(false) } }
  async function restore(): Promise<void> { setBusy(true); setError(''); try { await window.devbox.backups.restoreLatest('RESTORE'); setConfirmation(''); await refetch(); window.location.reload() } catch(reason) { setError(reason instanceof Error?reason.message:'Restore failed.') } finally { setBusy(false) } }
  return <div className="backup-settings"><div><h2>GitHub backup</h2><p>Back up all non-secret local DevBox data to an existing private GitHub repository.</p></div>{githubName ? <p className="backup-integration">Using connected GitHub integration: <strong>{githubName}</strong></p> : <p>Connect a GitHub integration, or use a backup-only token below.</p>}<p className="backup-warning">Backups are not encrypted. Anyone with access to the private repository can read cached work titles, URLs, settings, and local work state. Tokens and Keychain secrets are never included.</p>{status?.repository ? <div className="backup-status"><strong>{status.repository.fullName}</strong><span>{status.lastBackedUpAt ? 'Last backed up '+new Date(status.lastBackedUpAt).toLocaleString() : 'No backup created yet'}{status.dirty ? ' · local changes pending' : ''}</span>{status.lastCommitUrl && <button className="secondary" onClick={() => void window.devbox.links.openExternal(status.lastCommitUrl!)}>Open backup<ExternalLink size={15} /></button>}<button onClick={() => void backup()} disabled={busy}>{busy ? 'Backing up…' : 'Back up now'}</button></div> : <><button className="secondary" onClick={() => void chooseRepository()} disabled={busy}>{busy ? 'Loading…' : githubName ? 'Use connected GitHub integration' : 'Check GitHub integrations'}</button>{repositories.length>0 && <div className="backup-config"><label htmlFor="backup-repository">Backup repository</label><select id="backup-repository" value={repositoryId} onChange={(event)=>setRepositoryId(event.target.value)}><option value="">Choose a repository</option>{repositories.map((repository)=><option key={repository.id} value={repository.id}>{repository.fullName}</option>)}</select><label className="backup-ack"><input type="checkbox" checked={acknowledged} onChange={(event)=>setAcknowledged(event.target.checked)} />I understand this private-repository backup is unencrypted.</label><button onClick={() => void configure()} disabled={busy||!repositoryId||!acknowledged}>Use this repository</button></div>}{repositories.length===0 && <form className="token-form backup-token-form" onSubmit={(event)=>void connectToken(event)}><label htmlFor="backup-token">Backup-only GitHub token</label><input id="backup-token" type="password" value={token} onChange={(event)=>setToken(event.target.value)} autoComplete="off" spellCheck={false} placeholder="github_pat_…" required disabled={busy} /><p>Use a fine-grained token with Contents read/write access to the private backup repository. It does not replace your existing integration.</p><button type="submit" disabled={busy}>{busy ? 'Verifying token…' : 'Use backup token'}</button></form>}</>}{status?.repository && <div className="backup-restore"><h3>Restore latest backup</h3><p>This replaces all local DevBox data. GitHub must be reconnected afterward because credentials are never restored.</p><input aria-label="Restore confirmation" value={confirmation} onChange={(event)=>setConfirmation(event.target.value)} placeholder="Type RESTORE" /><button className="danger-button" onClick={() => void restore()} disabled={busy||confirmation!=='RESTORE'}>Restore latest</button></div>}{error && <p className="error">{error}</p>}</div>
}

function GlobalSettingsView({ theme, onToggle, githubConnected }: { theme: Theme; onToggle: () => void; githubConnected: boolean }): React.ReactElement { return <section className="settings-view"><p className="eyebrow">Applies to DevBox</p><h1>Global settings</h1><div><div><h2>Appearance</h2><p>Choose the contrast for every workspace on this Mac.</p></div><button onClick={onToggle}>{theme === 'dark' ? 'Use light mode' : 'Use dark mode'}</button></div><BackupSettings githubName={githubConnected ? 'your connected account' : undefined} /></section> }
function WorkspaceSettingsView({ workspace, done }: { workspace: Workspace; done: () => void }): React.ReactElement { const [error,setError]=useState(''); async function archive(): Promise<void> { try { await window.devbox.workspaces.archive(workspace.id); done() } catch(reason) { setError(reason instanceof Error?reason.message:'Unable to archive workspace.') } } return <section className="settings-view workspace-settings-view"><p className="eyebrow">This workspace</p><h1>Workspace settings</h1><div className="workspace-settings-section"><WorkspaceForm current={workspace} done={done} /></div><div className="workspace-settings-section danger-zone"><div><h2>Archive workspace</h2><p>Hide {workspace.name} from the workspace switcher. Its local data is kept for restoration.</p></div><button className="danger-button" onClick={() => void archive()}><Archive size={16} />Archive</button></div>{error&&<p className="error">{error}</p>}</section> }

function App(): React.ReactElement {
  const [page, setPage] = useState<Page>('Overview')
  const [theme, setTheme] = useState<Theme>('dark')
  const [refreshing, setRefreshing] = useState(false)
  const [menuOpen, setMenuOpen] = useState(false)
  const [creating, setCreating] = useState(false)
  const [sidebarWidth, setSidebarWidth] = useState(() => {
    const stored = Number(window.localStorage.getItem('devbox:sidebar-width:v1'))
    return Number.isFinite(stored) ? Math.min(MAX_SIDEBAR_WIDTH, Math.max(MIN_SIDEBAR_WIDTH, stored)) : DEFAULT_SIDEBAR_WIDTH
  })
  const [isResizing, setIsResizing] = useState(false)
  const resizeStart = useRef<{ pointerX: number; width: number } | null>(null)
  const { data: workspaces = [], refetch: refetchWorkspaces } = useQuery({ queryKey: ['workspaces'], queryFn: () => window.devbox.workspaces.list() })
  const { data: current, refetch: refetchCurrent } = useQuery({ queryKey: ['current'], queryFn: () => window.devbox.workspaces.current() })
  const { data: globalIntegration, refetch: refetchIntegrations } = useQuery({ queryKey: ['github-global'], queryFn: () => window.devbox.github.global() })
  const { data: jiraIntegration, refetch: refetchJira } = useQuery({ queryKey: ['jira', current?.id], enabled: !!current, queryFn: () => window.devbox.jira.get(current!.id) })
  const { data: assignedRepositories = [] } = useQuery({ queryKey: ['github-assigned', current?.id], enabled: !!current, queryFn: () => window.devbox.github.assignedRepositories(current!.id) })
  const { data: githubItems = [], refetch: refetchItems } = useQuery({ queryKey: ['items', current?.id], enabled: !!current, queryFn: () => window.devbox.github.workItems(current!.id) })
  const { data: jiraItems = [], refetch: refetchJiraItems } = useQuery({ queryKey: ['jira-items', current?.id], enabled: !!current, queryFn: () => window.devbox.jira.workItems(current!.id) })
  const items = useMemo(() => [...githubItems,...jiraItems].sort((a,b) => ({urgent:0,high:1,normal:2}[a.priority]-{urgent:0,high:1,normal:2}[b.priority] || b.updatedAt.localeCompare(a.updatedAt))), [githubItems,jiraItems])
  const integration = globalIntegration ?? undefined
  const refreshData = (): Promise<unknown[]> => Promise.all([refetchIntegrations(), refetchJira(), refetchItems(), refetchJiraItems(), refetchWorkspaces()])
  const refresh = (): void => { void (async () => { setRefreshing(true); try { if (current && globalIntegration) await window.devbox.github.refreshWorkspace(current.id); if (current && jiraIntegration) await window.devbox.jira.refresh(current.id); await Promise.all([refetchIntegrations(), refetchJira(), refetchItems(), refetchJiraItems(), refetchWorkspaces()]) } finally { setRefreshing(false) } })() }
  const switchWorkspace = (id: string): void => { void window.devbox.workspaces.select(id).then(() => { setMenuOpen(false); setCreating(false); void refetchCurrent(); void refetchWorkspaces() }) }
  const finishWorkspaceChange = (): void => { setMenuOpen(false); setCreating(false); void refetchCurrent(); void refetchWorkspaces() }
  // An archive can move the current-workspace pointer in the main process.
  // If a renderer observes the list before that pointer, recover to an active
  // workspace rather than showing first-run onboarding.
  useEffect(() => { if (!current && workspaces[0]) switchWorkspace(workspaces[0].id) }, [current, workspaces])
  const title = useMemo(() => page === 'GitHub' ? 'Git' : page === 'GitIntegration' ? 'GitHub integration' : page === 'Jira' ? 'Tickets' : page === 'Bitbucket' ? 'Git' : page === 'GlobalSettings' ? 'Global settings' : page === 'GlobalIntegrations' ? 'Global integrations' : page === 'WorkspaceSettings' ? 'Workspace settings' : page, [page])
  const clampSidebarWidth = useCallback((width: number): number => Math.min(MAX_SIDEBAR_WIDTH, Math.max(MIN_SIDEBAR_WIDTH, width)), [])
  const startResize = useCallback((event: React.PointerEvent<HTMLDivElement>): void => {
    if (window.innerWidth <= 760) return
    event.preventDefault()
    event.currentTarget.focus()
    resizeStart.current = { pointerX: event.clientX, width: sidebarWidth }
    setIsResizing(true)
  }, [sidebarWidth])
  const resizeWithKeyboard = useCallback((event: React.KeyboardEvent<HTMLDivElement>): void => {
    const direction = event.key === 'ArrowLeft' ? -1 : event.key === 'ArrowRight' ? 1 : 0
    if (!direction) return
    event.preventDefault()
    setSidebarWidth((width) => clampSidebarWidth(width + direction * 16))
  }, [clampSidebarWidth])
  useEffect(() => window.devbox.events.subscribeWorkspaceChanges(refreshData), [])
  useEffect(() => { setTheme((window.localStorage.getItem('devbox:global-theme:v1') as Theme | null) ?? 'dark') }, [])
  useEffect(() => { document.documentElement.dataset.theme = theme }, [theme])
  useEffect(() => { window.localStorage.setItem('devbox:sidebar-width:v1', String(sidebarWidth)) }, [sidebarWidth])
  useEffect(() => {
    if (!isResizing) return
    const move = (event: PointerEvent): void => {
      if (!resizeStart.current) return
      setSidebarWidth(clampSidebarWidth(resizeStart.current.width + event.clientX - resizeStart.current.pointerX))
    }
    const stop = (): void => { resizeStart.current = null; setIsResizing(false) }
    window.addEventListener('pointermove', move)
    window.addEventListener('pointerup', stop)
    return () => { window.removeEventListener('pointermove', move); window.removeEventListener('pointerup', stop) }
  }, [clampSidebarWidth, isResizing])
  if (!current) return <main className="welcome"><div className="welcome-mark"><AppMark /></div><h1>Create your first workspace</h1><p>Keep each organization’s developer attention separate and local to this Mac.</p><WorkspaceForm done={() => void refetchCurrent()} /></main>
  const hasConfiguredSource = assignedRepositories.length > 0 || !!jiraIntegration?.projects.length
  return <div className={'app-shell ' + (isResizing ? 'is-resizing' : '')} style={{ '--workspace-sidebar-width': sidebarWidth + 'px' } as React.CSSProperties}><WorkspaceRail current={current} workspaces={workspaces} select={switchWorkspace} integrations={() => setPage('Integrations')} settings={() => setPage('GlobalSettings')} theme={theme} toggleTheme={() => { const next = theme === 'dark' ? 'light' : 'dark'; setTheme(next); window.localStorage.setItem('devbox:global-theme:v1',next) }} /><Sidebar current={current} workspaces={workspaces} page={page} integration={integration} jira={jiraIntegration??undefined} assignedRepositories={assignedRepositories} menuOpen={menuOpen} creating={creating} setMenuOpen={setMenuOpen} setCreating={setCreating} setPage={setPage} select={switchWorkspace} done={finishWorkspaceChange} /><div className="sidebar-resizer" role="separator" aria-label="Resize workspace sidebar" aria-orientation="vertical" aria-valuemin={MIN_SIDEBAR_WIDTH} aria-valuemax={MAX_SIDEBAR_WIDTH} aria-valuenow={sidebarWidth} tabIndex={0} onPointerDown={startResize} onKeyDown={resizeWithKeyboard} /><main className="main-content"><header className="topbar"><div className="breadcrumb"><span>{current.name}</span><ChevronDown size={15} /><b>{title}</b></div><button className="topbar-button" onClick={refresh} aria-label="Refresh workspace" disabled={refreshing}><RefreshCw className={refreshing ? 'spin' : ''} size={17} />{refreshing ? 'Refreshing…' : 'Refresh'}</button></header><div className="content-body">{page === 'GlobalIntegrations' ? <GitHubConnect integration={globalIntegration ?? undefined} assignedRepositories={[]} refresh={refreshData} /> : page === 'Integrations' ? <IntegrationDirectory integration={integration} jira={jiraIntegration??undefined} assignedRepositories={assignedRepositories} openGitHub={() => setPage('GitIntegration')} openJira={() => setPage('Jira')} /> : page === 'GitIntegration' ? <GitHubConnect integration={integration} workspaceId={current.id} assignedRepositories={assignedRepositories} refresh={refreshData} /> : page === 'GitHub' ? <GitView repository={assignedRepositories[0]} integration={integration} items={githubItems} manageIntegration={() => setPage('GitIntegration')} /> : page === 'Jira' ? <JiraConnect workspaceId={current.id} integration={jiraIntegration??undefined} refresh={refreshData} /> : page === 'Bitbucket' ? <PlannedTool name="Bitbucket" category="Git" /> : page === 'GlobalSettings' ? <GlobalSettingsView theme={theme} githubConnected={!!globalIntegration} onToggle={() => { const next = theme === 'dark' ? 'light' : 'dark'; setTheme(next); window.localStorage.setItem('devbox:global-theme:v1',next) }} /> : page === 'WorkspaceSettings' ? <WorkspaceSettingsView workspace={current} done={finishWorkspaceChange} /> : <WorkView items={items} hasConfiguredSources={hasConfiguredSource} goToIntegrations={() => setPage('Integrations')} />}</div></main></div>
}

if (typeof window.devbox === 'undefined') window.devbox = createBrowserDevboxApi()
createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={client}><App /></QueryClientProvider></React.StrictMode>)
